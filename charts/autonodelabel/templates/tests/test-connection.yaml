apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autonodelabel.fullname" . }}-test-connection"
  labels:
    {{- include "autonodelabel.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autonodelabel.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
