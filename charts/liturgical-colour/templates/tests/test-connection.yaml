apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "liturgical-colour.fullname" . }}-test-connection"
  labels:
    {{- include "liturgical-colour.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "liturgical-colour.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
