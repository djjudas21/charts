apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "jump.fullname" . }}-test-connection"
  labels:
    {{- include "jump.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "jump.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
