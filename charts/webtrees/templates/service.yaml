apiVersion: v1
kind: Service
metadata:
  name: {{ include "webtrees.fullname" . }}
  labels:
    {{- include "webtrees.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "webtrees.selectorLabels" . | nindent 4 }}
