apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "connectbox-exporter.fullname" . }}-test-connection"
  labels:
    {{- include "connectbox-exporter.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "connectbox-exporter.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
