apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "espocrm.fullname" . }}-test-connection"
  labels:
    {{- include "espocrm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "espocrm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
