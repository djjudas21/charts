apiVersion: v1
kind: Service
metadata:
  name: {{ include "espocrm.fullname" . }}
  labels:
    {{- include "espocrm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "espocrm.selectorLabels" . | nindent 4 }}
