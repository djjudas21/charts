apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "spoolman.fullname" . }}-test-connection"
  labels:
    {{- include "spoolman.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "spoolman.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
